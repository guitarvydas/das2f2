function rf () {
    var bytes = fs.readFileSync ('d2f.drawio', 'utf-8');
    console.log (`rf len=${bytes.length} type=${typeof bytes}`);
    var data = atob (bytes);
    console.log (`rf atob data len=${data.length} type=${typeof data}`)
